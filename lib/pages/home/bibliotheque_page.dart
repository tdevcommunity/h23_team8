import 'package:flutter/material.dart';

class BibiothequePage extends StatelessWidget {
  const BibiothequePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("BIBLI", style: TextStyle(color: Colors.black, fontSize: 45))
      ],
    );
  }
}
